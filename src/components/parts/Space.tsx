import { View, StyleSheet } from "react-native";

export const StandardSpace = () => {
  return (
    <View style={styles.space}></View>
  );
}

const styles = StyleSheet.create({
  space: {
    height: 20,
  }
});